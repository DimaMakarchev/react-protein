import React from "react";
import preLoad from '../resource/preloader.svg';

export const Preloader = (props: any) => {
    return (
        <>
            <img src={preLoad} alt={'ok'}/>
        </>
    )
}
